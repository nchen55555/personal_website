import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Static output — deploys zero-config to Vercel.
  // Set `site` to your production URL once you have a domain (used by RSS/canonical URLs later).
  // site: 'https://your-domain.com',
});
