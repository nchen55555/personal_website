{
  "name": "personal-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "notes:encrypt": "node --env-file-if-exists=.env scripts/notes.mjs encrypt",
    "notes:decrypt": "node --env-file-if-exists=.env scripts/notes.mjs decrypt"
  },
  "dependencies": {
    "@fontsource/carlito": "^5.3.0",
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "marked": "^18.0.13"
  }
}
